# v0.41 打包前实验验收 TODO

日期：2026-05-31

状态：mock/synthetic acceptance path 已完成；bounded live provider smoke 已在单独授权后完成并通过复核。

## 目标

验证 v0.41 不是纸面集成，而是可以通过统一入口跑通主要 workflow：

```text
clean package
  -> synthetic full build
  -> graph build
  -> query
  -> incremental human review
  -> release acceptance report
```

核心验收对象：

```powershell
python -m tools.workflow_runner ...
```

## 0. 验收原则

- [x] 所有主流程优先从 `python -m tools.workflow_runner ...` 进入。
- [x] 不用聊天上下文补命令，关键命令和产物写入报告。
- [x] 每一步产生 `workflow_manifest.json` / `workflow_report.md` 或对应 runner manifest。
- [x] mock smoke 只证明链路，不证明质量。
- [x] live provider 单独 gated，不混进默认验收。
- [x] 不写 durable memory。
- [x] 不写 graph truth。
- [x] 不启用 S3。
- [x] 不把 current view / `graph_current/` / visualization 当 truth。
- [x] 不把 graph metrics / community reports 当 support proof。
- [x] 不把 mock/regex graph extraction 当主 fidelity path。

## 1. Clean Package 验收

目标：确认打包后不是“源码目录能跑，安装后不能跑”。

TODO：

- [x] 新建干净目录或使用 public mirror。
- [x] 创建 venv。
- [x] 执行 `python -m pip install -e .[test]`。
- [x] 执行 `python -m tools.workflow_runner --help`。
- [x] 准备空 workspace。
- [x] 执行：

```powershell
python -m tools.workflow_runner status `
  --workspace <empty_workspace>
```

验收标准：

- [x] 安装成功。
- [x] 子包 `tools.graph` / `tools.maintenance` / `tools.step1` / `tools.step2` 可导入。
- [x] `workflow_runner` 可从模块方式启动。
- [x] 空 workspace 状态检查能生成 workflow manifest/report。
- [x] 不依赖 `users/`、`.env`、私有 provider 输出或外部下载资源。

## 2. Synthetic Full Build Smoke

目标：从统一入口跑 S1/S2 build/index，不触碰真实 provider。

TODO：

- [x] 准备 public-safe synthetic workspace。
- [x] workspace 含最小 S0B/raw organization 输入。
- [x] 执行：

```powershell
python -m tools.workflow_runner build-full `
  --workspace users\_v041_release_acceptance_mock_20260530 `
  --modeled-user-id "Mira Chen" `
  --target-participant "Mira Chen" `
  --proposal-provider mock `
  --duplicate-policy overwrite_generated `
  --run-id v041-build-full-mock-acceptance-v4
```

验收标准：

- [x] S1 Build 完成。
- [x] S1 Index 完成。
- [x] S2 Build 完成。
- [x] S2 Index 完成。
- [x] workflow manifest 记录四个步骤。
- [x] 没有 live provider call。
- [x] 没有 durable memory write。
- [x] 没有 graph truth write。
- [x] 输出可被后续 graph build 使用。

关键 manifest：

```text
users/_v041_release_acceptance_mock_20260530/workflow_runs/v041-build-full-mock-acceptance-v4/workflow_manifest.json
```

## 3. Graph Build Smoke

目标：验证 v0.3 图链路从统一入口跑通。

TODO：

- [x] 在同一 workspace 上执行：

```powershell
python -m tools.workflow_runner build-graph `
  --workspace users\_v041_release_acceptance_mock_20260530 `
  --provider mock_regex_baseline `
  --max-items 20 `
  --visual `
  --duplicate-policy overwrite_generated `
  --run-id v041-build-graph-mock-acceptance-v4
```

验收标准：

- [x] graph construction packets 生成。
- [x] graph route decisions 生成。
- [x] graph extraction candidates 生成。
- [x] consolidation 生成 node/edge/claim/evidence tables。
- [x] NetworkX utility 跑通。
- [x] profile/community 跑通。
- [x] final quality gate 跑通并 pass。
- [x] visual review bundle 跑通。
- [x] report 明确 mock/regex 不是主 fidelity path。
- [x] `graph_is_not_proof=true` 边界存在。
- [x] 不写 graph truth。
- [x] 不写 durable memory。

关键 manifest：

```text
users/_v041_release_acceptance_mock_20260530/workflow_runs/v041-build-graph-mock-acceptance-v4/workflow_manifest.json
```

## 4. Query Smoke

目标：验证查询从统一入口进入，并能使用图分支。

TODO：

- [x] 准备问题。
- [x] 执行：

```powershell
python -m tools.workflow_runner query `
  --workspace users\_v041_release_acceptance_mock_20260530 `
  --question "Mira 为什么需要 v0.41 workflow runner？" `
  --run-id v041-query-smoke-acceptance-v3
```

验收标准：

- [x] query output 生成。
- [x] 自动发现 v0.3 hash 后缀 graph dir。
- [x] 输出 `s23_prompt_context.md`。
- [x] 查询路径包含 lexical / embedding / graph 的融合逻辑。
- [x] prompt view 保持轻量，不塞完整证据链。
- [x] graph context 不被描述为 proof。

关键 manifest：

```text
users/_v041_release_acceptance_mock_20260530/workflow_runs/v041-query-smoke-acceptance-v3/workflow_manifest.json
```

## 5. Incremental Human Review 验收

目标：验证真实人工审核 gate，而不是只跑 auto smoke。

TODO：

- [x] 执行：

```powershell
python -m tools.workflow_runner incremental `
  --workspace users\_v041_release_acceptance_mock_20260530 `
  --run-id v041-incremental-prepare-acceptance-v2 `
  prepare `
  --reset `
  --port 8765
```

- [x] 确认生成 `HUMAN_REVIEW_INSTRUCTIONS.md`。
- [x] 在没有提交审核前尝试 finalize。
- [x] 确认 finalize 失败。
- [x] 打开 WebUI，人工审核 S1/S2/graph review items。
- [x] 点击 submit / finalize review session。
- [x] 再执行：

```powershell
python -m tools.workflow_runner incremental `
  --workspace users\_v041_release_acceptance_mock_20260530 `
  --run-id v041-incremental-finalize-acceptance-v1 `
  finalize
```

验收标准：

- [x] 未审核 finalize 必须失败。
- [x] 审核后 finalize 成功。
- [x] S1 current view 发布。
- [x] S2 current view 发布。
- [x] graph current 发布。
- [x] dependency map 生成。
- [x] query invalidation 生成。
- [x] incremental visual review 刷新。
- [x] 旧行被标记 historical/stale/excluded，不是静默删除。
- [x] current view 不被报告为 graph truth 或 durable truth。

关键 manifest：

```text
users/_v041_release_acceptance_mock_20260530/workflow_runs/v041-incremental-finalize-acceptance-v1/workflow_manifest.json
```

## 6. Bounded Live Provider 验收

状态：已执行；仍保持 gated，不进入默认 CI 或 public mirror。

目标：确认真实 provider path 没坏，但不做大规模质量判断。

前置条件：

- [x] 用户明确授权 live provider。
- [x] `.env` 或环境变量已正确配置；本轮实际可用模型为 `claude-haiku-4-5`。
- [x] 不把 provider 输出提交到 public mirror。

TODO：

- [x] 小批 S1/S2：

```powershell
python -m tools.workflow_runner build-full `
  --workspace users\_v041_live_provider_smoke_20260531_01 `
  --modeled-user-id "Mira Chen" `
  --target-participant "Mira Chen" `
  --proposal-provider openai `
  --allow-live-api `
  --max-items 3 `
  --provider-concurrency 2 `
  --duplicate-policy overwrite_generated `
  --run-id v041-live-build-full-smoke-v1
```

- [x] 小批 graph：

```powershell
python -m tools.workflow_runner build-graph `
  --workspace users\_v041_live_provider_smoke_20260531_01 `
  --provider openai `
  --api-mode chat_completions `
  --allow-live-api `
  --max-items 18 `
  --provider-concurrency 4 `
  --duplicate-policy overwrite_generated `
  --run-id v041-live-build-graph-llm-smoke-v1
```

验收标准：

- [x] provider call inputs 落盘。
- [x] provider call results 落盘。
- [x] schema parse 成功。
- [x] failures 即使出现也落到 failures 文件，不中断审计；本轮最终 smoke failure count 为 0。
- [x] manifest 记录 provider / model / `live_api_enabled`。
- [x] 不把 live 输出写成 truth。
- [x] 不写 durable memory。
- [x] 不写 graph truth。

关键记录：

```text
docs/v0.41-live-provider-smoke-report.md
users/_v041_live_provider_smoke_20260531_01/workflow_runs/v041-live-build-full-smoke-v1/workflow_manifest.json
users/_v041_live_provider_smoke_20260531_01/workflow_runs/v041-live-build-graph-llm-smoke-v1/workflow_manifest.json
```

## 7. Release Acceptance Report

目标：生成打包前最终验收报告。

TODO：

- [x] 新增 `docs/v0.41-release-acceptance-report.md`。
- [x] 写明每一步命令。
- [x] 写明 workspace。
- [x] 写明通过/失败。
- [x] 链接关键 manifest/report。
- [x] 写明已知风险。
- [x] 写明是否可以打包。
- [x] 写明 public package 不包含真实数据、`.env`、provider 输出、外部资源。

报告必须明确：

- [x] mock smoke 只证明链路。
- [x] live provider smoke 仍是 gated。
- [x] graph/current view/visualization 不是 truth。
- [x] S3/support checker 不在本轮范围内。

报告：

```text
docs/v0.41-release-acceptance-report.md
```

## 测试

全量测试：

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
python -m unittest discover -s tests
```

结果：

```text
Ran 322 tests in 124.785s
OK
```

## 当前结论

```text
v0.41 usable-alpha workflow integration accepted on mock/synthetic acceptance path.
```

仍不能宣称：

```text
production-ready
quality-proven live provider system
durable memory writing system
graph truth system
```
